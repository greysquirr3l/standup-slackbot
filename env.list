#Slack API token
$SLACK_TOKEN

#The Slack channel to use for standups
$STANDUP_CHANNEL

#Slack channel is private
$PRIVATE_CHANNEL

#The time standup should start in 24hr 00:00 format
$STANDUP_TIME

#The name of the bot owner.
$OWNER

#The name of the bot owner slack channel
$OWNER_CHANNEL

The standup length time in minutes (default 60)
$STANDUP_LENGTH_MINS

The HTTP port to listen on (default 8080)
$HTTP_PORT

The timezone IANA format e.g. Europe/London (default "America/Detroit")
$TIME_ZONE

#do a standup immediately at launch? (default false)
$DO_STANDUP_IMMEDIATELY
